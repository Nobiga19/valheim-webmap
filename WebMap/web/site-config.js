// The one file a deployment edits. Served by the mod itself, the viewer is
// relative to its own origin and wears the world's name; a hosted copy of
// the site sets its API base, render path, brand, links and status feed here.
window.XNV = { api: "", base: "/map.jpg" };
